feat: Integración de LeakCanary, GitHub Action para compilación de APK Debug sin caché y generador de keystore

- Integración de LeakCanary (v2.14) como herramienta de depuración en tiempo real en la compilación de depuración (debugImplementation) para detección y perfilado de fugas de memoria en el teléfono sin requerir PC.
- Creación del flujo de GitHub Actions `.github/workflows/build-debug-apk.yml` para compilar automáticamente el APK Debug descargando todo el proyecto, configurando el toolchain de Rust (sandbox_engine), NDK 26.1, CMake 3.22.1 y el runtime nativo de Lua 5.4 / C++17.
- Compilación con firma generada dentro del propio runner y sin caché (--no-build-cache, cache-disabled y --no-daemon) para asegurar builds limpios e íntegros.
- Creación del script `scripts/ensure-debug-keystore.sh` que previene bloqueos por falta de keystore en CI y obliga a generarla desde cero con keytool de Java en el mismo entorno del Action.
- Exportación y publicación automática del APK Debug generado y firmado como artefacto descargable en GitHub Actions.
- Actualización de la documentación técnica en README.md, ROADMAP.md, STRUCTURE.md y AI_CONTEXT.md reflejando la infraestructura de depuración y CI/CD añadida.
